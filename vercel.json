{
  "buildCommand": "npm install --legacy-peer-deps && npm run build",
  "installCommand": "npm install --legacy-peer-deps",
  "functions": {
    "src/app/api/**": {
      "maxDuration": 30
    }
  }
}